#include "Stack.h"
#include <iostream>
using namespace std;

void Stack::push(const int& val)
{
	int* t=new int[index+1];

	for(int i=0;i<index;i++)
		t[i]=Top[i];
	t[index]=val;
	delete Top;
	Top=t;
}

int Stack::top() const
{
	if(index>0)
		return Top[index-1];
	return 0;
}
